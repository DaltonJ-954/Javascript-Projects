<!DOCTYPE html>
<html>
  <head>
    <style>
      @keyframes example {
        0% {
          transform: translateX(0%);
        }
        100% {
          transform: translateX(100%);
        }
      }

      .box {
        width: 100px;
        height: 100px;
        background-color: blue;
        animation: example 2s linear infinite;
      }
    </style>
  </head>
  <body>
    <div class="box"></div>
  </body>
</html>
This HTML page creates a blue box that animates from left to right using a keyframe animation called "example". The animation lasts 2 seconds, has a linear timing function, and repeats indefinitely.




Regenerate response
